apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: drone
  name: drone
spec:
  type: NodePort
  ports:
    - name: "80"
      port: 80
      targetPort: 80
  selector:
    io.kompose.service: drone
